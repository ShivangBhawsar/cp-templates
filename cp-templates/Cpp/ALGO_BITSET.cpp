const ll M = 5000;
bitset<M> bs;

// string s = "101001";
// bs = bitset<M>(s);
// bs.to_string();
// bs.count(); // returns number of set bits in bitset
// bs.set(); // sets all bits in the bitset
// bs[i]; // access or assign ith bit
// bs.to_ullong(); //convert to unsigned long long
// bs[0] gives the rightmost index
// m & (m-1) turns off the lowest bit that was set to 1 in a positive number m
// bitset<17> BS;
// BS[1] = BS[7] = 1;
// cout<<BS._Find_first()<<endl; // prints 1
// bs._Find_next(idx). This function returns first set bit after index idx
// bitset<17>BS;
// BS[1] = BS[7] = 1;
// cout<<BS._Find_next(1)<<','<<BS._Find_next(3)<<endl; // prints 7,7
// https://codeforces.com/blog/entry/43718
// https://codeforces.com/blog/entry/73558